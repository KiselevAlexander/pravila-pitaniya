import React from 'react';

export const Footer = ({children}) => (
    <footer className="mainFooter">
        <div className="logo">
            <a href="/">

            </a>
        </div>
        {children}
    </footer>
);